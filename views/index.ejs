<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task manager</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-screen min-h-screen bg-zinc-900 main">
        <h1 class=" manager text-white ">Task manager</h1>
        <div class="p-7">
            <form action="/create" method="post" >
                <input type="text" placeholder="Title Here" class="block px-5 py-3 bg-zinc-800 mb-3 rounded-md text-sm w-[50%] outline-none text-white" name="title">
                <input type="text" placeholder="Write details about task" class="block h-20 px-5 bg-zinc-800 mb-3 rounded-md text-sm w-[50%] outline-none text-white" name="about">
                
                <input type="submit" value="create task" class="text-white mt-2 bg-zinc-500 px-3 py-2 rounded-md text-xs font-medium hover:bg-zinc-600 hover:cursor-pointer">
            </form>
        </div>
        <div class="p-7 flex flex-wrap gap-4">
            <% if(files.length > 0) { %>
                <% files.forEach(function(val){ %>
                    <div class=" min-w-72 text-white bg-zinc-800 rounded-md hover:bg-violet-500 box-hover cursor-pointer">
                        <h2 class="p-3 rounded-md text-sm tracking-tighter"><%= val %></h2>
                        <div class="flex items-center justify-between">
                            <a href="/file/<%= val %>" class="text-violet-500 inline-block mt-3 text-xs p-3">Read More</a>
                            <a href="/edit/<%= val %>" class="text-zinc-500 inline-block mt-3 text-xs p-3">Edit Filename</a>

                        </div>
                    </div>      
               <% }) %>
           <% } else{ %>
                <h1 class="text-zinc-500 text-xl  font-semibold">No Tasks Yet :(</h1>
         <%  } %>
        </div>
    </div>
</body>
</html>